[project]
name = "ai-bills-routing"
version = "0.1.0"
description = "Account-aware AI routing and durable spending admission"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27,<1"]

[dependency-groups]
dev = ["pytest>=8,<9"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
